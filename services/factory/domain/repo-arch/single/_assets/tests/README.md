# Tests

This directory contains the tests that start the complete component.

Keep a test here when it checks the component from the outside, for example through its API or
its user interface. Keep a unit test next to the code that it checks, in `src/`.
