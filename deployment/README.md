# Deployment

This directory contains deployment configuration and related files.

Keep deployment code in its own repository when it has a separate release or access process. Document the target environment and the steps needed to deploy it.
